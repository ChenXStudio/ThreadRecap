# Codex thread writeback feasibility

## Decision

**NO-GO / BLOCKED** under the Stop-time transcript-completeness hard gate.

The stable app-server writeback gate passed with Codex CLI `0.144.2`: a real
non-ephemeral CLI session was located from its hook `session_id`, resumed as
the same thread, and received a persisted marked turn. However, a repeated
real hook probe showed that the current turn's explicit `task_complete` record
was not yet in the transcript when `Stop` ran. It appeared only after the CLI
process finished. Therefore Stop-time current-turn completion is not proven.

The main implementation must not proceed on the prior GO. The later cooldown
state machine and recursive-trigger guard also remain unimplemented.

## Supported surface used

The probe followed the current official Codex manual and the JSON schema
generated by the tested CLI version. It used only:

- hook input fields documented for `SessionStart`, `UserPromptSubmit`, and
  `Stop`;
- app-server JSONL `initialize` / `initialized`;
- `thread/list`, `thread/resume`, `turn/start`, `turn/completed`, and
  `thread/read`;
- stable `clientUserMessageId`, persisted as the user message `clientId`.

It did not enable `experimentalApi`, automate a UI, or read an unpublished
database. The manual explicitly says the transcript format is not stable. The
Stop check therefore records only derived booleans from the observed `0.144.2`
JSONL shape and does not expose or treat that shape as a production contract.

## TDD evidence

Tests were added before either spike module existed.

RED:

```text
py -3 -m pytest tests/test_spikes.py -q
ERROR during collection: No module named 'spikes'
```

After the initial implementation, eight tests passed and the concurrent JSONL
test exposed a Windows lock race. The lock was corrected and the focused suite
passed. A later test-first hardening cycle demonstrated that the common
unseparated field name `apikey` was not redacted; that focused test failed
before the suffix-safe redaction rule was added.

Final focused result:

```text
9 passed
```

Coverage includes recursive sensitive-field redaction, concurrent complete-line
JSONL append, Stop-time transcript state, internal-trigger recognition,
JSON-RPC response ID pairing, notification routing, and the complete stable
thread resume/write/read protocol flow.

Review hardening added a second RED run with `7 failed, 6 passed`. It proved
that plural/password/credential keys leaked, a `.lock` side file was created,
the CLI accepted a directory override, and newline-only transcript checks did
not prove turn completion. The first fix reached `12 passed, 1 failed`; the
remaining failure showed that bare multi-handle `O_APPEND` on Windows lost
records. Locking the target JSONL itself while keeping one `os.write` reached
`13 passed`, preserved all 80 concurrent test records, and created no side
file.

Final repository verification completed with `27 passed`; the official plugin
validator, bytecode compilation, `git diff --check`, path/credential scan, and
probe cleanup check also passed.

## Real hook probe

A temporary ignored marketplace copy was installed through the normal plugin
marketplace flow. Only that copy received the probe `hook_entry.py`; the
repository's production hooks configuration and future Task 8 entry point were
not changed. The vetted copy was exercised with:

```text
codex exec --dangerously-bypass-hook-trust --sandbox read-only --json <benign prompt>
```

No approvals-and-sandbox bypass was used. The command created a persisted
thread and completed normally.

The three captured records belonged to one session and had these sanitized
observations:

| Event | `session_id` | `turn_id` | `transcript_path` | `cwd` | Event-specific |
| --- | --- | --- | --- | --- | --- |
| `SessionStart` | string, UUID-shaped | absent | string | string | `source` was `startup` |
| `UserPromptSubmit` | string, UUID-shaped | string, UUID-shaped | string | string | event name matched |
| `Stop` | string, UUID-shaped | string, UUID-shaped | string | string | JSONL was valid and current turn was present, but matching `task_complete` was absent at Stop |

Every record also contained a string capture timestamp. Recursive scanning
confirmed that singular/plural token, key, authorization, cookie, secret,
password, and credential variants held only the redaction placeholder. No real
prompt, identifier, transcript, path, credential, or captured JSONL is
included in this report.

The repeated real Stop probe found all nonempty transcript lines parseable as
JSON and found records carrying the current `turn_id`. At Stop, no matching
`event_msg` / `task_complete` existed. After process exit, that exact
turn-scoped completion record was present. Agent text is not an explicit
completion record, so it is not used as substitute evidence.

## Real app-server probe

The probe accepted an explicit `--codex` binary and also supports normal PATH
discovery; no machine-specific executable path is stored in the repository.
Using the captured session ID, the stable JSONL flow completed within its
bounded timeout with all checks true:

```text
initialized=true
session_thread_mapping=true
thread_id_matches_session_id=true
non_ephemeral=true
resumed_same_thread=true
turn_completed=true
internal_client_id_persisted=true
result_in_marked_turn=true
```

The submitted visible input was:

```text
⏱ 会话已冷却，使用 $thread-recap 生成阶段摘要
```

`turn/start.clientUserMessageId` used a `thread-recap-internal:` prefix.
`thread/read` persisted that value as `userMessage.clientId`; the corresponding
completed turn also contained an agent message. This gives the later state
machine a stable field for identifying an internally generated turn and a
stable turn boundary for associating its result.

## Failure modes and remaining work

- **Blocking result:** Stop-time current-turn completion is unproven because
  the explicit matching completion record is written after Stop returns.
- A missing, ambiguous, ephemeral, or differently resumed thread is a NO-GO.
- A missing matching `turn/completed`, missing persisted client ID, or missing
  result in the marked turn is a NO-GO.
- app-server startup and model work can be slow; the probe uses a bounded
  timeout and reports sanitized error codes.
- The real CLI run observed unrelated local MCP startup timeout warnings, but
  the target turn completed and the stable protocol checks passed.
- The app-server mapping and writeback result remains positive, but it does not
  override the Stop evidence failure.
- Task 8 would still need to verify complete recursive-trigger behavior if a
  revised design resolves this blocker.

## Cleanup

Both temporary probe installations, marketplace registrations, marketplace
copies, plugin-data directories, and real hook captures were removed after
their runs. Generated schemas and other ignored feasibility inputs were also
removed; only the SDD progress and task reports remain in ignored scratch.
The repository hooks file was unchanged.
